<?xml version="1.0" encoding="utf-8"?>
<!--
    A PC cell: an icon over a label, ringed as a whole. The ring is the FrameLayout's
    foreground, which needs a FrameLayout to work below API 23; the padding gives the ring
    its breathing room, and the RelativeLayout inside lays the icon and label out as before.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="20dp"
    android:foreground="@drawable/pc_focus_ring"
    android:focusable="true"
    android:clickable="true">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <RelativeLayout
            android:id="@+id/grid_image_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">
            <ImageView
                android:id="@+id/grid_image"
                android:layout_centerHorizontal="true"
                android:layout_width="125dp"
                android:layout_height="125dp" />
            <ImageView
                android:id="@+id/grid_overlay"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="28dp"
                android:layout_width="50dp"
                android:layout_height="50dp" />
            <ProgressBar
                android:id="@+id/grid_spinner"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="28dp"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:indeterminate="true" />
        </RelativeLayout>

        <TextView
            android:id="@+id/grid_text"
            android:layout_width="125dp"
            android:layout_height="wrap_content"
            android:layout_below="@id/grid_image_layout"
            android:layout_marginTop="10dp"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:textSize="18sp" />

    </RelativeLayout>

</FrameLayout>
