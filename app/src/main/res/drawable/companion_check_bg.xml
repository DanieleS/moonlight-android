<?xml version="1.0" encoding="utf-8"?>
<!--
    The ground the picker's check sits on. An app icon is arbitrary art, so the mark needs
    its own disc of the panel's surface to stay legible over anything it lands on.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/surface_raised" />
    <stroke
        android:width="1dp"
        android:color="@color/signal" />
</shape>
