<?xml version="1.0" encoding="utf-8"?>
<!-- The ring around the gamepad button letter in an on-screen hint. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/content_muted" />
</shape>
